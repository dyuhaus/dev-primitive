Run the builder over the direct API (billed to the configured provider key, not this session's subscription). Write the planner's plan verbatim plus any decisions from step 2 to a temp file, then run via Bash: `{{DEV_PRIMITIVE_DIR}}/bin/role-call builder --task-file <that file>`. Builder turns can run many minutes — run it in the background and wait for completion, or use the maximum Bash timeout (600000 ms). Its stdout is the build report; the stderr `[role-call]` line reports the run's API cost — include that figure in your final report. If it exits non-zero, surface its error message to the user verbatim and offer the remedies it names (provision the key with `{{DEV_PRIMITIVE_DIR}}/bin/set-api-key`, or temporarily re-route building with `python3 {{DEV_PRIMITIVE_DIR}}/apply.py set builder {{BUILDER_MODEL}} --access harness`). Do not silently do the building yourself.
