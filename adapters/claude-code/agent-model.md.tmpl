---
description: Switch {{AGENT_DISPLAY_NAME}}'s model (now {{AGENT_MODEL}}) and regenerate — wraps `apply.py set {{AGENT_KEY}}`.
argument-hint: "<model class>, or --id <exact id>, or blank to show current"
---

<!-- Generated from dev-primitive/roles.config.json by apply.py. Do not hand-edit;
     change agents.{{AGENT_KEY}} and re-run: python3 apply.py claude -->

Change which model the **{{AGENT_DISPLAY_NAME}}** profile (`{{AGENT_KEY}}`)
uses. Thin wrapper over the primitive's `set` command — `roles.config.json`
stays the single source of truth and the adapters are regenerated for you.

**Requested change:** $ARGUMENTS

Do this:

1. If `$ARGUMENTS` is empty, run `python3 /home/dyadmin/dev-primitive/apply.py show`,
   report this agent's current row, then stop — change nothing.
2. Otherwise run exactly:
   `python3 /home/dyadmin/dev-primitive/apply.py set {{AGENT_KEY}} $ARGUMENTS`
   A bare value sets the model **class** and clears any pin; `--id <id>` pins an
   exact model; `--provider <name>` changes provider; `--no-apply` updates the
   config without regenerating; `--dry-run` previews. The command validates
   before writing, so an invalid value aborts without changing anything.
3. Relay the resolved table it prints and confirm this agent now resolves to the
   intended model. Relay any validation error verbatim instead.

Scope of this command:

- It edits the **shared** registry that Claude Code, Codex, and generic
  consumers resolve. It does **not** change Pi's overlay at
  `adapters/pi/roles.config.pi.json`; use Pi's own `/{{AGENT_KEY}}-model`
  command for that.
- Pointing this profile at a **non-Anthropic** provider does not make the
  subagent run there — this harness cannot dispatch other providers for a
  subagent. The generated agent will instead run on the session model and route
  its verdict through `scripts/external_review.py`. Say so plainly if the
  requested change crosses provider families, and verify afterward with:
  `python3 /home/dyadmin/dev-primitive/scripts/external_review.py --check --profile {{AGENT_KEY}}`

Never hand-edit the generated agent files — `set` is the only supported path.
