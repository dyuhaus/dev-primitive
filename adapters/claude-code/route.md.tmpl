---
description: Explainably recommend the appropriate configured agent, then confirm before invoking it.
---

<!-- Generated by dev-primitive/apply.py. Do not hand-edit. -->

# Route a task

For the supplied task, run the deterministic local router; it makes no model
call and **does not dispatch** work:

```bash
python3 /home/dyadmin/dev-primitive/router.py --explain "$ARGUMENTS"
```

Present the selected profile, confidence, reasons, alternatives, and any
clarifying questions. The system recognizes applicable agents but is always
**confirmation-required**: ask the user before calling the selected agent. Never
auto-invoke Team Leader: it is direct-call-only and may be used only after an
explicit user request.
