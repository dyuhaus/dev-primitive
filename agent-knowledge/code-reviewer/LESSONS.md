# code-reviewer lessons

This is durable, harness-neutral working knowledge for the `code-reviewer` profile.
Keep behavioral, reusable lessons here; do **not** store secrets, credentials,
personal data, private task content, or chronological task logs.

## Durable practices

- Read the profile and applicable project instructions before work.

## Dated lessons

<!-- Append at most one evidence-backed, generalized entry after substantive work:
- YYYY-MM-DD | task type | reusable lesson | evidence/path or validation command
When this section reaches 50 entries, fold the oldest reusable items into Durable
practices and remove the consolidated dated entries. -->

- 2026-07-29 | live-infra rename review | Two candidate findings in one review were artifacts of the evidence-gathering step, not the code: grep -o of <link> tags made commented-out stylesheets look active (raw file read showed the <!-- -->), and a probe loop that prepended the host to already-absolute hrefs produced false 404s on pages that were live-200. Before reporting, re-verify any extraction-based evidence (grep -o, generated probe loops) against raw file content and a hand-built request. | verified via sed -n on index.html + direct curl of each URL
- 2026-07-29 | credential/identity toolchain review | When reviewing wrappers that inject a machine-account identity, do not stop at the code paths: verify the STORED identity data against the remote's actual attribution (git log author emails on the branch, then `gh api repos/<owner>/<repo>/commits/<sha> -q .author.login`). A valid token paired with a wrongly-registered author email silently reattributes every commit to the human account while every script "works". Also empirically test fail-open on empty (not just missing) credential files — `gh` treats empty GH_TOKEN as unset and falls back to the ambient login. | validated: git credential fill with/without helper reset + commits API on homelab PR 12
- 2026-07-29 | game-economy branch review | When a stated invariant rests on a table of DECLARED constants that the test suite merely multiplies (credit tables, pool budgets), recompute each declaration's actual sustained value from the runtime code path before trusting any green check: averaging the per-round multipliers of a burst/every-Nth mechanic exposed a ~1.30x declared-vs-actual gap that every closed-form assert passed, and the same method (a two-element counterexample trace) showed an invariant-repair loop cascading in the wrong direction while its comment claimed the opposite. Checks that assert the declaration can never see the implementation. | verified by hand arithmetic against the emit/burst code and a trace of the repair loop; star-salvage feat/v4-weapon-trees
- 2026-07-29 | tool-removal branch review | On a "remove tool X" branch, template/script grep is not sufficient: check the repo's OWN seeded instances of the files the branch only stops seeding (root config file, root AGENTS.md rendered from the old template), and check whether the idempotent adoption tooling will ever heal already-seeded copies fleet-wide (an "append only if section missing" guard means existing repos keep stale instructions forever). | verified: branch removed templates/repo-seed/coderabbit.yaml yet tracked /.coderabbit.yaml + AGENTS.md:30 survived in the same repo, and 37 fleet repos kept the stale seeded text
- 2026-07-29 | deploy-coupled relay review | When a PR claims "merge = live" because a timer runs the script from a checkout, verify the checkout's ACTUAL branch and commit (`git branch --show-current`, `rev-parse origin/<default>`) and the live journal before trusting the claim: here the timer's "master checkout" was parked on an unrelated feature branch one commit behind origin/master, so merging would deploy nothing and the removed integration kept running live. Pair this with a scan for constants that survive while their enforcement was deleted (a cap defined but no longer applied is a silent-truncation bug, not dead code). | validated: systemctl cat + journalctl -t dy-pr-relay + grep of the running file vs the PR worktree; SUMMARY_MAX declared-unused proved by 6000-char build_notification probe
- 2026-07-29 | autonomous-merge gate review | When a diff delegates a safety property to something OUTSIDE the diff (branch protection, a config file's current value, a gh JSON field), verify each backstop live instead of trusting the PR narrative: here the body claimed "public repos are protected" (all sampled were 404 Branch not protected), the author gate lived in a config whose parse failure silently resets it to permissive defaults, and `gh pr view --json files` silently caps at 100 files (proved with a 115-file PR returning 100) — three load-bearing assumptions, all falsifiable with one bounded command each. Also probe free-text classifier gates with a table of honest-but-unlisted phrases ("N/A", "TBD", "pending"); a denylist gate passes everything it never imagined. | gh api branches/.../protection 404s + load_config repro + k8s PR 140782 changedFiles=115 vs files=100
- 2026-07-29 | stale-base PR review | When a PR's base is behind the default branch, review the MERGE RESULT, not the diff: `git merge-tree --write-tree <master> <head>` finds textual conflicts, and checking out the merged tree in a scratchpad worktree lets you compile and inspect the auto-merged files — two individually-correct branches auto-merged into a semantically wrong artifact (a new BigTextStyle summaryText silently replacing an accurate setSubText added by the intervening PR). Also: before accepting a "never compiled, reviewed-by-eye" caveat, check for an on-machine toolchain (local.properties/sdk.dir) — a bounded `gradlew :app:compileDebugKotlin` of both the head and the merged tree discharged it in minutes. | validated on personal-android-app PR 17: merge-tree showed bridge.js conflict; compileDebugKotlin exit 0 on head and merge
- 2026-07-29 | fleet-migration script review | When a script both `git rm`s a path and later `git add`s a shared CHANGED list, test all three file states in a sandboxed run of the real script (stub HOME/gh, throwaway repos): tracked-clean crashed the whole run (`git add` of an already-rm'ed pathspec is fatal under set -e), untracked was silently re-COMMITTED instead of removed, and tracked-dirty was destroyed with no backup — three different bugs from one line. Also pin the review target with `git show <branch>:<file>` into scratch; a shared checkout was switched to main by another process mid-review and the first test run silently executed the wrong version (caught only because output strings didn't match the code just read). | validated via fakehome harness, scratchpad repos t1-t10, homelab fix/repo-adopt-propagation 8f69d0f
- 2026-07-29 | sold-product whole-codebase audit | For packaged/sold software, audit the doc-advertised deployment SCENARIOS against the storage model, not just the code: the worst finding was one doc line recommending classroom-with-sync while the server stored a single last-write-wins state blob (guaranteed learner data loss), and the second-worst was an unvalidated twin of a validated input path (file import checked the course tag and shape; the sync pull of the same data did not, letting crafted JSON reach innerHTML). Also scan the BUILT artifact and full git history for the known-real secrets (hash the canonical credential and grep for it computationally, never printing it) rather than only pattern-grepping the worktree. | verified: training-console-template DEPLOY.md:177 vs server.js one-blob design; storage.js:44 check absent from state.js initSync; zero hits for the real password/hash across repo+history+zips
- 2026-07-29 | trading-system whole-codebase audit | For any HTTP client that authenticates via a query parameter, every `error: str(exc)` log line is a credential writer: requests embeds the full URL (including the query string) in ConnectionError/HTTPError messages, so the secret lands verbatim in events/telemetry JSONL. Trace each logged error field to every consumer -- a dashboard that tails a state file into an unauthenticated /api/*.json endpoint publishes it. Prove it without printing: regex-extract the value, compare equality against the env-file source, and report counts/positions only. Small append-only event files make such rows effectively permanent (an 18-row file never leaves a tail-160 window). | verified: key-bearing rows written 2026-06-29 were still being served on a public status API a month later
- 2026-07-30 | systemd-sandbox / guard-rewrite re-review | A `ReadOnlyPaths=` entry nested inside a `ReadWritePaths=` parent is a bind mount that DETACHES when the file is replaced from outside the namespace: after an atomic rewrite the path silently becomes writable and deletable again inside the jail. Test any such "read-only exception" with a three-phase probe (write-attempt, outside `mv -f` replace, write-attempt again) rather than the single write-attempt that shows it working. Pair this with `stat`'s `Birth` field to prove whether the protected file is replaced or edited in place (Birth == Modify means a new inode, so atomic replace), and check whether the same read-only parent breaks the app's OWN writes — a config the tool rewrites atomically cannot be saved when its parent directory is read-only, and the failure is silent (exit 0, unchanged inode+mtime). | verified on autopilot feat/autopilot-core: probe unit regained write+delete after an outside rename, `claude doctor` changed .claude.json's inode outside the jail but not inside, daemon.log showed ~28560s proactive credential refreshes
- 2026-07-30 | fix-of-a-fix guard re-review (round 3) | When a fix closes a laundering route for ONE member of a category, enumerate the whole category and test each member: a shell-stdin refusal gated on `is_shell` (POSIX shells only) left python3/node/perl/ruby/php reading their program from stdin (`echo '...' | python3 -`, or bare `python3`) fully unchecked -- the identical bypass one interpreter over. Separately, replacing an exact-flag match with a broadened REGEX can REGRESS cases the exact match handled: `TARGET_DIR_RE=^-[a-zA-Z]*t$` made `rsync -rt/-at/-t SRC DST` return the SOURCE as the copy destination (rsync `-t`=preserve-times, not target-dir), so the real out-of-repo DEST was never checked -- the old exact `-t` match let bundles fall through to positional[-1] correctly. And a field-concatenation fix (`result + errors[]` for classification) creates new false positives when the appended text feeds a substring regex: a genuine crash whose framework error mentions `rate_limit`/`limit reached` now classifies as a usage-limit and, because that outcome sets no wait_until and charges no attempt, livelocks the queue instead of failing after max_attempts. Verify each via the real interface (guard subprocess, classify() with crafted envelopes), and check the DOWNSTREAM state handler (apply_result) not just the classifier. | verified on autopilot feat/autopilot-core b1723ef: gharness.py showed python3/node/perl stdin ALLOW vs sh deny, rsync -rt/-at/-t to /home/dyadmin/homelab ALLOW vs -a/-avz deny, copy_destination(['-rt','src/','.../homelab/'])='src/', classify(error_during_execution,['...rate_limit.py...'])=limit
- 2026-07-30 | fourth-pass guard/state-machine re-review | An EXEMPTION added to a security refusal needs the same category enumeration as the refusal itself, tested against the real tools rather than the flag's common meaning: an `INFO_ONLY_ARGS={--version,--help,-V,-h}` carve-out re-opened a closed stdin-laundering route for every interpreter, because `-h` is bash's hashall option and `-V` is dash/sh's vi-edit mode (both still execute the piped program), and `python3|perl|node - --version` puts the flag in argv while reading the program from stdin. Separately, when reviewing a human-in-the-loop escalation, do not stop at "does it escalate" -- drive the FULL round trip (escalate, answer, next tick) and check whether the predicate's inputs are persistent counters the release path never resets; here answering re-escalated immediately and re-notified, so the operator could never unblock the task. Pair both with a check of whether the outer kernel/sandbox layer actually backstops the specific abuse: filesystem writes were covered, but network-side operations, credential reads and glob spellings of denied paths were not. | verified on autopilot feat/autopilot-core 9269d10: guard subprocess ALLOWs `echo "gh pr merge 3" | bash -h` and `cat ~/.claude/.cred*`; systemd-run probe showed .ssh/.gnupg/appdata sealed (mode 000) while .credentials.json and .config/gh/hosts.yml stayed readable; 3 answers produced 3 questions + 3 pushes
- 2026-07-30 | merge-resolution re-review | When a PR claims "resolved by taking master wholesale and re-applying N changes", one pair of commands proves or disproves the whole claim: `git merge-base master HEAD` must equal the master tip (branch fully contains master), and a two-dot `git diff master HEAD` full-tree stat must show ONLY the intended files/hunks — that subsumes symbol-by-symbol hunting for each master feature the merge could have dropped. Then run master's pre-existing suites against the merged tree (they are the regression oracle the diff can't be). For version-skew on a serialized model, check all four cells of the matrix by locating the DECODE config (ignoreUnknownKeys) and the ENCODE types separately — an app that decodes the shared model but encodes dedicated request DTOs is skew-safe in both directions by construction. | validated on personal-android-app PR 17: merge-base == dda220b == master tip, 2-dot diff = 4 files/3 bridge hunks, 10 suites green
- 2026-07-30 | rebuilt fail-closed merge-gate re-review | When a gate is rebuilt to "fail closed on wrong types", probe the TYPE of every individual knob, not just the container: an outer-object type check plus truthiness on the inner flag meant the strings "false"/"no"/"off" ARMED autonomy while the docstring promised the opposite, and the pinning test only covered the container. Probe every count-vs-list guard with the degenerate empty list (an `and paths` clause made zero visibility bypass the "refuse when truncated" refusal that partial visibility triggered), and re-enumerate any kept denylist in first person ("did not review" passed while "not reviewed" was listed). Finally, read the REFUSAL REASON the suite actually logs: two headline negative tests were multiply-refused fixtures (empty rollup + count mismatch) whose named gate could regress green. | verified via direct load_config/autonomy_decision/review_state probes + suite run under a gh/gh-agent PATH trap on personal-android-app feat/relay-autonomy-policy 638f9a1
- 2026-07-30 | markdown section-rewrite re-review | When a script rewrites "one section" of a markdown file with a hand-rolled awk parser, drive it with the grammar the parser does NOT model, not just the shapes its comments mention: an ATX-only end rule deleted setext (`Title\n----`) and 1-3-space-indented headings below the section, a ```-only fence rule mangled ~~~ blocks, and one unterminated fence swallowed everything to EOF — each producing a committed, pushable mutilation. Also check that the PRESENCE check and the MUTATOR share one parser: a fence-blind detector plus a fence-aware replacer created a "reported UPDATED, file unchanged" path whose empty commit killed the run under set -e; the same empty-commit sink was reachable from an untracked-file retirement (plain rm stages nothing). | validated via sandboxed runs (fake HOME, stubbed gh/git-agent, local bare origins) t1-t13/b1-b3/c1/e1/f1/g1 on homelab fix/repo-adopt-propagation 86ec49b
- 2026-07-30 | autonomous-merge gate round-3 re-review | Two recurring shapes: (a) when a gate's evidence is in-band self-attestation, check whether an OUT-OF-BAND human signal already exists that the gate simply never reads — here `gh pr view --json reviewDecision` carries an explicit human CHANGES_REQUESTED verdict the merge path never requests, so the gate merges over a standing human rejection while parsing prose for "was it reviewed"; and (b) after a strict-type fix lands on ONE policy knob (`enabled is True`), probe every sibling knob the same way — `requireReview: null/0/""` silently waived the review requirement and the success reason then logged "reviewed" for a PR with no review section at all. Also re-probe rebuilt free-text classifiers one TENSE over, not just one synonym over: past-negation ("did not review") was closed while future deferral ("will review in the follow-up", "review deferred/owed", "punting on review") all passed and self-merged end-to-end. | verified via review_state/load_config/run() probes with stubbed merge + read-only gh field check on personal-android-app feat/relay-autonomy-policy d3a5d57
- 2026-07-30 | security-port branch review | A sync-pull guard that "fails closed" by refusing an unrecognized remote AND scheduling a push of local state is a destructive write whenever local is the empty/fresh cell of the matrix: drive the fresh-client + legacy-remote combination explicitly (stub fetch, capture the PUT body) — here the refusal of an untagged pre-patch server blob pushed `{cards:{}, examAttempts:[], updatedAt:1970}` over the learner's only server copy while the commit message claimed "nothing is lost". Same review: re-pin HEAD before final reporting (the branch gained a second commit mid-review that fixed two defects already found), and probe lenient parsers used as validators with hostile-but-valid inputs — V8's Date.parse accepts "May 1 2026 (<markup>)" so a keep-as-is string branch stored markup its own suite asserted could never be stored. | verified via node module probes on code-fluency/loop-engineering fix/console-xss-and-bind (fb305ff/502013d)
- 2026-07-30 | security-fix port review (sync/import validator) | A fail-closed identity check on a sync path must be driven end-to-end through the LEGACY (untagged) payload case *including the debounced push*, not just the refusal: refusing an untagged remote and then unconditionally schedulePush()ing local bypasses the updatedAt comparison and overwrites the server copy with the stale/empty local one — the exact inverse of the patch's "fails closed, nothing is lost" claim, and invisible because the branch had zero tests for the pull itself (only for the stamping half). Two companion checks paid off: (a) source-regex tests for a bind/config constant pass a regression that leaves the constant unused — patching `server.listen(PORT, "0.0.0.0")` while keeping `const HOST = process.env.HOST || "127.0.0.1"` kept the whole suite green; assert on the call site or on behavior; (b) a "no markup can survive" validator that returns a string field verbatim after `Date.parse` still stores payloads, because V8's legacy date parser treats parenthesised trailing text as a comment (`Jan 1 2000 (<img src=x onerror=...>)` parses). Verify a loopback-default bind against the real orchestration (peer container over the docker network + published port), and check whether the healthcheck probes container-internal loopback — if so the outage reports healthy. | verified on pp-dev-associate fix/console-xss-and-bind b69ffb0: stubbed-fetch initSync probe (30 cards+1 attempt+1 lesson -> 3/0/0, server updatedAt moved backward), regressed-suite run exit 0, Date.parse probe, docker run with/without HOST=0.0.0.0
- 2026-07-30 | fleet-migration script re-review (round 3) | A pathspec commit (`git commit -- <paths>`) records the WORKING-TREE state of those paths, so a "user staging is never swept in" guarantee fails exactly on the paths the tool itself owns (staged user edits to the tool's own files ride into the machine commit while the tool prints the opposite), and a `git add` performed before a commit that can fail (partial-commit refusal in a mid-merge/cherry-pick repo) leaves machine changes in the user's index where the next human commit silently absorbs them; a re-run that derives "nothing to commit" from this-run template comparisons then reports adopted forever over the uncommitted residue. Drive such reviews with dirty/staged/mid-merge fixtures on the tool's OWN paths, probe hand-rolled markdown parsers one grammar level deeper each round (CRLF defeats $-anchored blocker regexes; indented ``` lines straddling a heading fool a shared naive fence toggle), and revert-test every advertised safety rule — here 4 of the branch's fixes (seed-drift propagation, denylist-missing fail-closed, deny key matching, the whole backlog push guard) left the 90-assert suite green when reverted. | validated via sandboxed repros s1-s8 + REPO_ADOPT_BIN revert runs on homelab fix/repo-adopt-propagation c40d222
- 2026-07-30 | preflight-guard + test-group review | For a diff that adds a refusal guard plus a test group claiming per-case reproduction, three bounded probes settle the whole review: (1) run the group against the BASE binary and tabulate per-case FAIL counts — a case with zero base failures is either vacuous or a deliberate differential pair (here J6b was the anti-over-correction half of J6, by design); (2) kill-test the guard's two core promises with targeted mutants (enumeration→truthiness fail-open; exemption removal), not a wholesale revert; (3) check report-field regexes for prefix vulnerability — '^Field +: no' also matches the sibling state "not checked", so a field assert can pass on the wrong state and only neighboring asserts close the hole. Also verify the stub answers only inputs the real tool would answer: a stub that resolves fixture-shaped values (local-path origins) the real gh rejects makes the suite's happy path more permissive than production. | validated via base-run FAIL table + 2 mutants on homelab fix/repo-adopt-archived-guard 788d24b
- 2026-07-30 | stale-security-fix landing review | When a PR's justification is "this fix has been live in ~/bin since <date>, main is just stale", byte-compare the LIVE artifact against both the branch and base versions before accepting the premise: here the live copy of the credential tool was mtime-stamped at the original install date and identical to the UNFIXED base, so the leak the PR claimed was already closed in production had been running the whole time, and merging (copies, not symlinks) would not deploy the fix either — the report must separate "merge deploys it" paths (checkout-coupled hook/timer script) from "merge changes nothing live" paths (~/bin copies). Also, when a hook/backup guard gains new deny globs, quantify the false-positive blast radius by running the exact matching semantics over every fleet repo's tracked files before fleet-wide adoption. | validated: cmp + stat on ~/bin vs git show origin/main:… and branch:…; fnmatch sweep found 20 tracked files newly blocked across 10 repos; homelab PR 22
- 2026-07-31 | procedural-geometry pipeline review | Two bounded probes settle most claims in a DCC batch pipeline: (1) `blender --background --python <raising script>; echo $?` — Blender exits 0 on an unhandled Python exception unless `--python-exit-code` is passed, so an assert-based "test suite fails the build" guarantee is false for every caller that gates on exit status; test the harness's exception→exit-code path, never assume it. (2) For a geometry fix documented only in comments (a clamp, skirt, falloff), kill-test it by zeroing its constant in the loaded module and diffing the generated vertices — a 16 mm "rim plunge" measured 0.02 mm of actual effect because a `> 0` mask excluded the one ring the constant targeted. Stub `bpy`/`mathutils` with tiny shims to exec the module in plain python; and probe node-socket subscripting live (lookup falls back to the socket IDENTIFIER and resolves duplicate names to the enabled typed socket), rather than reasoning from socket-name tables. | verified: probe_fail.py exit 0 on Blender 5.2; EAR_SKIRT 0.016→0 vertex delta max 0.0205 mm; outputs["Fac"]→name "Factor"
- 2026-07-31 | glTF-export guard review | When reviewing a guard that asserts PBR factors exist in an exported glTF, read the exporter's factor-gathering code first: Blender's io_scene_gltf2 returns `fac if fac != 1 else None` for metallic AND roughness, so an absent factor is ambiguous between "node-driven" and "literal at the spec default", metallic's default is 1.0 (full metal) making its omission the dangerous silent direction, and a legit literal-1.0 roughness false-positives a presence check. Also probe the two vacuous paths: an empty/absent materials array skips the loop entirely, and a primitive with no material index renders as default white metal while every per-material check passes — verify the committed GLB's primitives actually carry material refs. For datablock-leak claims, decompress the committed .blend (zstd since 3.x) and count name occurrences rather than trusting save-order reasoning alone. | verified: pbr_metallic_roughness.py:195,268 in Blender 5.2; GLB JSON chunk parse; zstd -d + count on anatomical_bust.blend
