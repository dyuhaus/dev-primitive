# code-reviewer lessons

This is durable, harness-neutral working knowledge for the `code-reviewer` profile.
Keep behavioral, reusable lessons here; do **not** store secrets, credentials,
personal data, private task content, or chronological task logs.

## Durable practices

- Read the profile and applicable project instructions before work.

## Dated lessons

<!-- Append at most one evidence-backed, generalized entry after substantive work:
- YYYY-MM-DD | task type | reusable lesson | evidence/path or validation command
When this section reaches 50 entries, fold the oldest reusable items into Durable
practices and remove the consolidated dated entries. -->

- 2026-07-29 | live-infra rename review | Two candidate findings in one review were artifacts of the evidence-gathering step, not the code: grep -o of <link> tags made commented-out stylesheets look active (raw file read showed the <!-- -->), and a probe loop that prepended the host to already-absolute hrefs produced false 404s on pages that were live-200. Before reporting, re-verify any extraction-based evidence (grep -o, generated probe loops) against raw file content and a hand-built request. | verified via sed -n on index.html + direct curl of each URL
- 2026-07-29 | credential/identity toolchain review | When reviewing wrappers that inject a machine-account identity, do not stop at the code paths: verify the STORED identity data against the remote's actual attribution (git log author emails on the branch, then `gh api repos/<owner>/<repo>/commits/<sha> -q .author.login`). A valid token paired with a wrongly-registered author email silently reattributes every commit to the human account while every script "works". Also empirically test fail-open on empty (not just missing) credential files — `gh` treats empty GH_TOKEN as unset and falls back to the ambient login. | validated: git credential fill with/without helper reset + commits API on homelab PR 12
- 2026-07-31 | live bind-mounted config review | For a config file that is single-file bind-mounted into a running container, the vendor's own validator run via `docker run --rm -v <candidate>:<path>:ro <image> ...` is the authoritative check (cloudflared has `tunnel --config ... ingress validate` and `ingress rule <url>` to prove exactly which rule a hostname now matches, with no credentials needed); and a `docker exec ... cat | cmp` probe of the live config can fail with rc=127 because distroless images ship no cat — treat that as exec failure, never as a config mismatch. When the working tree IS the deploy, also flag the pre-merge window: until the branch merges, any `git checkout main` in that repo re-arms the old config on the next container restart. | verified: cloudflared ingress validate OK + "Matched rule #15 http_status:404" on homelab chore/models-site-offline; exec cat rc=127
- 2026-08-12 | provenance/permission-model review | A provenance marker stamped into user-visible, copyable content (e.g. a UUID in a note's frontmatter) is NOT a capability boundary: verbatim duplication (an editor's "make a copy", a template, a sync) reproduces the marker with no write access to the gated resource, so a derived user artifact is then treated as server-owned and becomes writable/destroyable. When a design says "forging the marker needs the write access the marker gates", check the non-forge path — copying — separately. Verified by replicating ownerIdOf() on a verbatim copy of a marked note: the copy reports the same owner id. | node repro of parseFrontmatter+ownerIdOf on a duplicated note; dy-mcp feat/vault-edit-own-notes
- 2026-08-12 | transport/CORS diff review | Responses written by an embedded SDK transport bypass the app's own header helper, so CORS/security headers added to a `json()`-style helper never reach the delegated route's success path — always `curl -D` the 200 of the SDK-written route, not just the framework's error responses (and remember Access-Control-Expose-Headers on a preflight is inert; browsers only honor it on the actual response). Separately, switching a transport from streaming to buffered-JSON mode (`enableJsonResponse`) can leave `handleRequest` as a never-settling promise on client abort because buffered cleanup deletes state without resolving and nothing watches the abort signal; prove or disprove with a paired repro run in both modes — and when the claim is a MEMORY leak, verify reachability with WeakRef + --expose-gc in a mirror whose post-await variable liveness matches the real code exactly, because V8 collects unreachable pending-promise chains: a never-settling awaited promise is a correctness bug (skipped post-await code) but not necessarily a leak. | dy-mcp fix/mcp-client-portability: settled=false (json) vs RESOLVED (sse); leak-pair.mjs showed BOTH shapes fully GC-collected once in-flight work finished
- 2026-08-12 | schema-guard diff review | A security guard that enumerates a zod schema's DECLARED keys (`.shape`) is vacuous against the widening forms — `.passthrough()`/`.catchall()` (v4: `looseObject`) keep a clean `.shape` while forwarding arbitrary undeclared keys into the handler's args. The invariant to assert is runtime, not structural: safeParse a canary key and require it absent from `.data`. Also verified: which zod family a schema lands in decides its emitted JSON Schema (SDK zod-compat routes v3 → zod-to-json-schema with additionalProperties:false, v4 → toJSONSchema with the key omitted), so a zod major upgrade silently reverts schema-shape fixes — pin the emitted schema with a tools/list-level test, which turns that revert into a loud failure. | node repro: shapeOf(z.object({}).passthrough()) = {} while .parse({cmd:'rm -rf /'}) retains cmd; dy-mcp fix/schema-additional-properties
- 2026-08-12 | schema-rewrite diff review | When a rewrite collapses `anyOf:[X,{type:"null"}]` by hoisting X's keywords beside `type:[X,"null"]`, type-SPECIFIC keywords (properties, required, minLength, additionalProperties) hoist equivalently, but type-AGNOSTIC assertions (`enum`, `const`) silently start rejecting null — prove both directions with ajv on the pre/post forms, and check the schema GENERATOR really emits the dangerous shape (zod-to-json-schema does for `.enum().nullable()` / `.literal().nullable()`, and it also emits draft-07 array-form `items`/`additionalItems` that keyword-list recursions typically miss). Also grade "fails loudly via a test" claims against where tests actually run: a repo with no CI whose deploy flow is pull→build→compose makes an ignored boolean return silent in prod. | ajv + tsx probes on dy-mcp fix/portable-tool-schemas; SDK emission checked via toJsonSchemaCompat directly
- 2026-08-13 | docs/version close-out review | In this machine's C-locale shells, grep without `-a` can silently emit ZERO matches on ordinary UTF-8 source files (em-dash/box-drawing bytes make grep classify them binary; file(1) says "data") — which fabricates negative evidence: a file that plainly exported a symbol looked like it defined nothing, one step from reporting a phantom missing-export defect. When a grep over a file you have independently seen content in returns nothing, rerun with `grep -a` before trusting the absence; prefer `-a` by default during review. Same failure hid a version literal in a built dist/ file from a repo-wide grep. | dy-mcp src/vault-client.ts: `grep FromEnv` = no output vs `grep -a FromEnv` = 3 hits
- 2026-08-15 | freeze-handoff docs review | When a document enumerates the units that carry some property ("the N timers with Persistent=true"), never verify the enumeration by re-reading the doc's sources — grep the property across ALL unit files in scope and diff the two sets. The omitted members are exactly the ones nobody thought about, and here one omission (a watchdog timer) inverted a guarantee the doc made ("resuming the pair together prevents the false alarm" — the watchdog's own catch-up run races the backup's and can still fire it). | grep -l 'Persistent=true' over /etc/systemd/system/*.timer + ~/.config/systemd/user/*.timer vs the doc's list; homelab a4a3ec0
- 2026-08-15 | auth-gateway CSRF review | SameSite=Strict is site-scoped (registrable domain / eTLD+1), not origin-scoped, so a hand-rolled gateway that leans on "SameSite blocks cross-site + we also refuse Sec-Fetch-Site: cross-site or a foreign Origin" still ships a hole: a request from a SIBLING subdomain (any *.same-domain host the defender may not fully control) is same-site, so the cookie IS sent, Sec-Fetch-Site is `same-site` (never `cross-site`), and GET/navigation/img/script/iframe/no-cors-fetch carry NO Origin header — so both guards pass and the request is proxied authenticated. POST is safe (browsers always attach Origin) and WebSocket is safe (handshake always carries Origin), which is exactly why the gap hides. When a doc claims "cross-site is refused," test the same-site case specifically, and require the initiator to be same-origin/none rather than merely not-cross-site. | homelab feat/dsh-web-gateway gate.js crossSiteMarked()==='cross-site' only + foreignOrigin() only fires when Origin present; verified cookie-strip/safeEqualStr and that Node rejects TE+CL inbound (HPE_INVALID_CONTENT_LENGTH) so the CL passthrough is latent-only under keepAlive:false
- 2026-08-15 | single-upstream proxy -> multi-client hub refactor review | When an authenticating reverse proxy grows a client registry + routing cookie, the load-bearing invariants do NOT transfer for free: re-verify them BEHAVIOURALLY against the NEWLY-SELECTED upstream, not by reading the diff. Stand up the real gate against two mock upstreams and assert, per selected client, that Host/Origin/Referer are rewritten to that client's loopback authority, ALL gateway-issued cookies (incl. the new routing cookie) are stripped, badInitiator still 403s same-site/cross-site/foreign-origin while allowing same-origin/none/absent, and the forced-password gate still precedes the hub routes. Two general traps found sound here but worth the check elsewhere: (a) a registry "loopback-only" SSRF guard must be tested against octal (0177.), decimal (2130706433), IPv6 (::1 vs unreachable [::1]), 0.0.0.0, trailing-dot and link-local (169.254.169.254) host forms — a numeric dotted-quad regex + parts[0]===127 rejected all the dangerous ones; and (b) a "reserved namespace can't be shadowed" claim enforced by exact/prefix match on an UN-decoded path (pathOf strips only the query) holds for canonical forms but silently routes encoded-slash (/hub%2fselect) and double-slash (//hub) variants to the proxied client — not exploitable when the client is already fully reachable by the authed user and the canonical escape-hatch path always wins, so grade the guarantee's PURPOSE (escape-hatch reachability + no capability gain), not its literal wording. | homelab feat/harness-hub: ran gate.js + 2 mocks, curl matrix confirmed strip/rewrite/badInitiator/gate; node repro of isLoopbackHost over 27 host forms; [::1] connect => ENOTFOUND
- 2026-08-16 | control-surface launch diff review | When a launcher flips focus:false -> focus:true on session/tab creation, the invariant "the service only writes to the pane it just created" covers the SERVICE's input path but not the HUMAN's: focus stealing redirects the attached operator's in-flight keystrokes into the fresh shell during the deliberate settle-sleep, and the service's own trailing Enter then executes whatever interleaved — so review the physical-input path separately from the request path. Also: validate an id-to-constant preset table with hasOwnProperty (bare index resolves __proto__/constructor), and when the upstream schema offers cwd/env params on the allowlisted create methods, confirm the diff never populates them from request data — the allowlist bounds METHODS, not params. | homelab cfa3388 opLaunch; herdr api schema --json TabCreateParams (cwd/env present, unused)
- 2026-08-16 | conflict-resolved re-landing review (ordering-sensitive config) | For a union-resolved re-landing of a live-deployed branch, the decisive evidence is three git-object comparisons with comments stripped: base vs branch tip (must be a pure superset), branch tip vs the original PR tip (cherry-pick lost nothing), and branch tip vs the live worktree (source-of-record really matches deploy). Here the only live-vs-branch delta was one rule block moved across another — in first-match-wins config a block move is safe IFF the match domains it crossed are disjoint, so verify disjointness (here: different hostnames) rather than assuming a move is cosmetic. Also grade a comment's defence-in-depth claim by reading the fallback layer's code (fail-closed token gate confirmed at the app layer), not by trusting the comment. | homelab fix/land-ihtc-offer-restore: diff <(git show A:f | grep -v '^\s*#') <(git show B:f ...) matrix + live curl matrix
- 2026-08-16 | path-prefix mount on an auth proxy review | For a `/mount/`-prefix router bolted onto an authenticating reverse proxy, three separable checks decide safety and the diff-read misses two: (a) auth ordering — resolveSession must precede matchMount in BOTH the request handler AND the `upgrade` handler; verify behaviourally that unauth GET/POST/WS to `/mount/*` returns login/401 with ZERO upstream hits (mock upstream logging every hit is the assertion). (b) the traversal guard is applied to the PATH remainder only (query-stripped), so the forwarded stripped URL still carries an unchecked query string; this is inert ONLY IF the upstream strips the query before routing and serves a fixed route allowlist (no filesystem-by-path) — grade the UPSTREAM's routing model, don't assume the guard covers the whole forwarded URL. (c) activation is 3-artifact-coupled: proxy routing code + the upstream's relative-URL emission + the registry `mountPath` entry must deploy together; enabling the mount in the registry while the upstream still emits root-absolute URLs breaks the page (its assets/control POSTs escape the mount to the cookie client) but does NOT un-authenticate it — a functional break, not a bypass, because the escaped control POST 404s at the wrong client. Single-segment lowercase-only mount pattern (`^/[a-z0-9][a-z0-9-]{0,30}/$`) plus a reserved-segment set, both enforced at registry-validation time AND out-ordered by the reserved-route checks in the handler, is defence in depth that holds against uppercase/unicode/multiseg/trailing-dot/duplicate mounts. | homelab feat/independent-client-tabs 76547a7: ran real gate.js + 2 mock upstreams, unauth matrix (7 verbs + WS) = 0 mock hits; same-site=>403, foreign-origin=>403, same-origin=>proxied w/ cookie stripped; matchMount over 34 traversal forms all 400/no-match; validateClients dropped all 9 malformed mounts
- 2026-08-16 | fifo/pipe IPC diff review | A "MAX_WRITE < PIPE_BUF, so one fifo write is atomic" comment is a two-part claim and both parts fail independently: (1) PIPE_BUF is 4096 on Linux (getconf PIPE_BUF /), so any cap above 4 KiB (here 32 KiB) already exceeds it — atomicity is only guaranteed for writes <= PIPE_BUF regardless of pipe capacity (default 64 KiB); and (2) atomicity is about interleaving concurrent writers, not torn single writes — a fd opened O_WRONLY|O_NONBLOCK lets write()/fs.writeSync do a PARTIAL write once the pipe is non-empty (returns bytes-written < len, no throw), and a full pipe throws EAGAIN. Code that ignores fs.writeSync's return value therefore silently drops the tail of a large line, corrupting a newline-delimited (NDJSON) protocol on the reader; the trigger is any burst that outpaces the reader's drain (an app whose send() gates only on state==='exited', not on in-flight, allows it via rapid POSTs even when the single-write case is safe). Verify with getconf PIPE_BUF + a repro: mkfifo, hold the read end open without draining, loop fs.writeSync of a >PIPE_BUF chunk on an O_NONBLOCK write fd — observe one full write, then a partial (< len), then EAGAIN. | maestro feat/phase-0-spine claude.js:48/480; repro returned [32769, 28673] then EAGAIN on node v22, PIPE_BUF=4096
- 2026-08-16 | agent-spawning-console (RCE-surface) review | For a "browser sends prompt TEXT, never a command" console, verify the two fences END TO END, not by trusting the comment: (1) the ONE command typed into the pane must be built only from constants + SERVER-GENERATED ids (random-hex sid, config-derived model) — confirm the create path never threads a request field (engine/model/cwd/env) into the adapter constructor (here createSession does `new Adapter({})`, so the browser cannot set model at all), and confirm every value in the typed string matches a strict no-metachar alphabet at its SOURCE (constructor + host-script re-check), since a guard right before the command may re-validate the dir/script but not the model; (2) "prompt is data" holds only if the delivery has zero shell layers — trace it to JSON.stringify -> fifo/stdin and read the host script for how the fifo is consumed (`exec 3<>in.fifo; claude <&3`, no eval/expansion). Also worth the check and found sound here: herdr method allowlist is vacuous if a call site interpolates a request-chosen method — grep every call site and confirm the method arg is a string literal (params too). Static serving used a single-segment allowlist regex (no `/`, no leading dot, known-ext) so path.join can't traverse; loopback bind confirmed live via `ss -tlnp sport=:8901`. No confirmed defect; the RCE is the accepted trust model gated by the auth gateway. | maestro feat/phase-0-spine a6c6eab: herdrCall sites all literal methods; command=`exec ${HOST_SCRIPT} ${dir} ${model}` all server-gen; ss showed 127.0.0.1:8901 only; live deploy diff -rq == branch
- 2026-08-16 | prompt-as-argv (RCE-surface) review | "spawn WITHOUT a shell + the prompt is a single execve argv element" defeats SHELL parsing but is NOT sufficient for the stated guarantee "prompt text can never become an argv option": the receiving CLI still runs its own getopt/commander parse, and a payload whose first byte is '-' is consumed as an OPTION, not the positional. Verified against dsh (commander with passThroughOptions/enablePositionalOptions/allowUnknownOption): `dsh --profile headless <task>` with task=`--dump-default-config` prints internal config, `-h` prints help, `--patch=/etc/hostname` makes the child OPEN+parse an arbitrary file as a YAML overlay (arbitrary file read / potential loader-overlay code load) — all from prompt text, no shell involved. Only reachable when the wrapper doesn't neutralize the leading dash: here the dsh path passes the raw prompt only on the FIRST turn (empty transcript); later turns prefix a "You are continuing…" string so the argv no longer starts with '-'. The fix class is an explicit `--` end-of-options separator or a guaranteed non-dash prefix. When reviewing "prompt is data not an argument," probe the ACTUAL target CLI with a leading-dash first positional (`-h`, `--patch=/path`) and read whether it end-of-options-guards. | maestro feat/phase-1: turn-host.js turnArgs dsh = ['--profile','headless',dshTask(text)] + dshTask returns raw text when transcript empty; spawnSync of the real dsh confirmed --patch=/etc/hostname opened the file
- 2026-08-16 | per-turn-engine driver (RCE-surface) review | When a host driver pass-throughs an engine's raw structured stdout (codex --json / pi --mode json) into the SAME events file that also carries the driver's own TRUSTED control markers keyed by a shared discriminator (`type:"maestro"`), the reader must namespace-separate them or filter the raw stream — otherwise an engine line whose parsed `.type==="maestro"` is interpreted by the backend as a driver marker (spoof host_exit/turn_end/user state). To grade REACHABILITY, check how the engine CLI frames MODEL output: codex/pi wrap the model's text as escaped JSON string VALUES inside their event envelope and emit one JSON object per line, so the model cannot emit a bare top-level line nor break NDJSON framing — the dangerous line can only come from the CLI itself, which never uses the app's private `maestro` namespace, so it is NOT browser-reachable (defense-in-depth, low). Separately, an id echoed from engine output and later placed in an argv OPTION position (codex `exec resume <thread_id>`) needs the SAME leading-dash ban as the model alphabet: a `/^[A-Za-z0-9-]{8,64}$/` thread-id regex still admits a leading `-` (verified: `-rf--json---help` matches), unlike `MODEL_PATTERN`'s `[A-Za-z0-9]` first-char anchor — latent only because the id is CLI-assigned, not model-assigned. And a "prompt can't smuggle a launcher flag" fix that framed the argv with a constant letter-initial prefix ONLY on the first turn must be re-checked to cover EVERY turn: here dshTask() now always prepends 'A'/'Y' framing, so the single execve argv element never begins with '-' regardless of prompt or history. | maestro feat/phase-1-engines-board: turn-host.js:336 record(raw) unfiltered + turnhost.js:203 normalizeMarker on type maestro; THREAD_ID_RE turn-host.js:138 node-repro true on leading dash; dshTask node-repro first byte A/Y for --dump-default-config/-h/--patch on turn 1 and N
- 2026-08-16 | replayed-event-stream action layer review | When a service enacts ACTIONS off a persisted event stream that is fully replayed on reattach, a content-hash dedupe (sid+message-text+index) has two inverse failure modes that a "replay can never re-enact" claim hides: (a) keys ABSENT — the first-ever scan of pre-existing history (feature's first deploy over old sessions, dedupe-file loss, or FIFO key eviction past the cap) enacts every stale directive as if fresh; (b) keys PRESENT — a genuinely re-issued byte-identical message is silently suppressed with no feedback, violating a "never a silent drop" guarantee, because content identity cannot distinguish replay from re-issue (an ordering/turn-anchor is needed). Separately: collectors re-armed in a single pass AFTER a sequential multi-session reattach loop race each session's already-started replay poll timer (first poll ≈250ms after attach; one slow sibling reattach — e.g. a 20s fifo-open retry — lets an earlier session replay its terminal event with no listener), so arm per-session at reattach time or gate the first poll on the re-arm pass. | maestro feat/phase-2-coordination coord.js scanAssistantMessage/rearm + server.js boot ordering; node repro of key collision

- 2026-08-16 | replay-safety FIX verification (event-stream action layer) | The structural fix for "replayed persisted events must not re-enact actions" is an ASYMMETRY plus an ORDERING, and both are verified by reading two specific spots, not the prose: (1) ASYMMETRY — the action scanner must early-return on `ev.replay===true` while the result collector must NOT (so a worker that finished during downtime is still collected off replay); grep both listeners and confirm only the action side checks the flag. (2) ORDERING — the replay tag must be set by the ADAPTER at tail-attach (bytes already on disk at `fstat().size` = replay; a line straddling the boundary completes as LIVE, the safe direction), and the per-session collector/scanner must be armed in the session-created hook that fires SYNCHRONOUSLY after reattach() returns and BEFORE the tail's first interval poll (here 250ms). The decisive check that closes the prior multi-session-reattach race: arming happens per-session inside the reattach loop body (fireCreated right after sessions.set), so a slow SIBLING reattach can't let an earlier session's poll fire with no listener — because that session's listener was attached before the loop yielded. Confirmed no state dependence: losing/corrupting the persistence file cannot re-enact (replay tag is adapter-derived, not persisted) — but note the inverse, that an all-or-nothing JSON load which discards every record on a single mid-write corruption then makes former workers unrecognized (isWorker()==false) and thus scanner-armed on the NEXT restart; still bounded (agent-driven depth-1 holds within a lifetime via the live map; only operator manual-prompting of a former worker after the loss exceeds depth-1, and the operator already has full authority). | maestro feat/phase-2-coordination 6d036b2: coord.js armScanner `if(ev.replay===true)return` vs armCollector.onEvent (no replay check); turnhost.js/claude.js _tailEvents replayUntil=fstatSync.size + per-session fireCreated in sessions.reattachExisting loop; save() writeFileSync non-atomic + load() catch→return
- 2026-08-16 | state-persistence hardening review | A loader that sniffs "legacy single-JSON-document" before falling back to NDJSON is ambiguous at exactly n=1: a one-record NDJSON file IS a valid single JSON document, so the legacy branch consumes it and returns [] — silently losing the record precisely in the most common early state (first pipeline/delegation ever). Always round-trip test save→load at n=0, 1, 2 records; n=1 is where every format-detection heuristic collapses. Grade the blast radius through the CONSUMERS of the load: here identity checks (isStep/isWorker) fed a session-created hook, so the lost record didn't just drop UI state — it re-classified a restricted session (pipeline step / worker) as an ordinary lead and armed the directive scanner the design promised it would never get. Second transferable defect in the same diff: a settle path that frees a capacity slot with an UNAWAITED async stop (herdr stop only marks exited after an await) and then synchronously re-acquires against the cap deterministically 429s at live==cap — at cap=1 every 2-step pipeline fails at step 2; repro'd by injecting require.cache mocks that preserve the real await ordering. | node repro: saveRecords 1 rec -> loadRecords []; cap harness on real pipelines.js -> "running-session cap reached (1/1)"; maestro feat/phase-3-role-pipelines
- 2026-08-16 | fix-branch verification (config->registry mapping + eligibility gates) | Two checks that reading the diff cannot settle: (1) a "validated against the FROZEN list, not the picker list" fix is proven by driving the module with a hostile config through its env override and asserting BOTH sides — the mapping refuses the config's own value AND the engine's picker list still contains it (showing the circular list is really the one being avoided, not that the hostile value never propagated); (2) an eligibility guard keyed on an external registry's literal enum value (`invocation === 'direct-call-only'`) is a fail-open BLOCKLIST — verify the exact key/value against the real registry file, and flag that any future rename or third enum value silently makes the excluded members eligible; the fail-closed form is allowlisting the known-eligible values. Also reusable: to verify a "stop must resolve before the next slot is acquired" chaining fix, have the mock stop() await a delay BEFORE flipping state (matching the real transport's round-trip) and log the live-count at every create — asserting completion alone would pass even if the fix merely widened the race. | maestro feat/phase-3-role-pipelines 260ee74: repro-roles.js picker=[evilmodel,...] vs registry refusal; repro-cap.js createLog all live==0 at cap=1
- 2026-08-16 | template-DAG engine (declarative-data-only) review | For a {{ref}}-interpolation engine, the purity proof is cheap and decisive: seed the UPSTREAM artifact with the template syntax itself plus every hostile alphabet ($(...), backticks, $&/$'/$`, leading '-', the engine's own directive marker, and the BEGIN/END bracket string) and count literal leftovers in the output — String.replace-with-callback passed all of it verbatim with zero re-scan. The invariant that actually broke was the BYTE BUDGET: a per-ref allocation of max(FLOOR, budget/count) inverts into total ≈ count×(FLOOR+markers) once count is large, and because the ref scanner counts every OCCURRENCE (duplicates of the same dep each get full markers), a 2-node graph with one edge and a repetitive template validates clean then deterministically exceeds the transport's hard send cap at run time. When a budget divides by ref count but floors the per-ref size, test with DUPLICATE refs, not distinct ones — distinct refs are bounded by the edge cap, duplicates only by template bytes. | maestro feat/phase-4-workflows: repro 60×{{a.output}} + 16KiB artifact → 38,099 B framed vs MAX_PROMPT_BYTES 32,768; adapters throw 'prompt is too long' → node failed post-spawn

- 2026-08-16 | byte-budget "validate-then-run" FIX + GC-safety verification (RCE-surface console) | A validate-then-run byte-budget fix is proven by two checks a diff-read cannot settle: (1) the framing magic-constants must be true UPPER bounds — measure the ACTUAL framed/marker text with worst-case ids (NODE_ID max len, MODEL_PATTERN max 64, MAX_NAME_CHARS name, longest role/purpose): here FRAME_OVERHEAD 900 vs actual 585, REF_MARKER 260 vs actual 246; (2) validate()'s refusal inequality must GUARANTEE interpolate()'s `Math.max(FLOOR, floor(numerator/count))` never has the floor bind — because validate refuses unless numerator/count >= FLOOR — so total framed stays <= budget; drive the REAL module across rising duplicate-ref counts and watch it flip from sized-down (framed < budget) to refused (400), never over-cap. Separately, a GC "can NEVER delete/prune live-or-running-referenced state" claim is cheap to prove behaviourally: write a crafted running-record state file (pipelines/workflows/delegations.json) referencing a sid, drop that sid's dir on disk, run the AGGRESSIVE purge ({keepMax:0,keepDays:0}), and assert the referenced dir survives while an unreferenced peer is deleted — the protectedSids = trackedSids ∪ runningReferencedSids set held, and the SID_RE filter on readdir names (before any path.join) plus the pre-meta-read protected check blocks traversal and protects in-memory sessions. Regression-suite teeth confirmed by 4 targeted mutations (byte-budget guard, token gate, scanner-eligibility predicate, model allowlist) each turning the suite red. | maestro feat/phase-5-polish 8bb9596: measure.js (constants), budget.js (flip point ~110 dupes), gc-safety.js (running-ref dir survived {0,0} purge), 4 git-checkout mutation runs
- 2026-08-16 | working-tree (no-git) pre-ship review | When reviewing a live working tree instead of a git snapshot, a GENERATED artifact can change under you mid-review — I nearly reported "manifest lists 1 of 30 lectures" as a launch-blocking defect while the generator was still running: the tell was the artifact's mtime being NEWER than my earlier read and newer than the newest input. Before reporting staleness/incompleteness in any generated file, re-stat + re-read it, compare its mtime against its inputs', and pgrep for a concurrent producer; state in the report WHICH snapshot (timestamp) was reviewed. Same review: a tree with no .git next to a git-backed sibling is itself a top-severity data-loss finding on this machine, independent of code quality. | ai-graph-engineering js/audio-manifest.js read at 1 entry, mtime 23:19:36 > newest mp3 23:19:34, re-read = 30 entries matching 30 lesson ids
- 2026-08-17 | offline-first app + new sync engine review | When a branch adds a background writer (lifecycle-triggered sync) to an app whose in-memory state is loaded asynchronously at startup, check the INITIALIZATION ordering, not just the mutex: a UI gated on "loaded" does not gate the background path, so a read-modify-write of the whole persisted state file (updateProgress reading _progress.value) launched from onStart can run against the EMPTY pre-load map and rewrite the file, permanently losing every slice the sync cannot restore. The tell is a state-load done in a LaunchedEffect/first-composition while the new writer fires in onStart, with the persistence write being whole-file replace. Also re-check any "unit tests drive X against a scripted transport" comment by listing the test dir — here the interface existed and the test did not, leaving never-PUT-without-GET and base-file discipline unexecuted. | training-companion feat/graph601-subject: MainActivity onStart syncIfEnabled vs AppNav LaunchedEffect loadProgress; src/test has no SyncManagerTest
- 2026-08-17 | "hermetic" auth test spawning the real server | A test that spawns the real server inherits every boot side-effect the server has, so grade a "hermetic: no network, no model call" claim by watching the spawned server's CHILD PROCESSES during the run (ps --ppid of the exact server pid — a pgrep -f pattern matches your own wrapper shell and lies) and by running once with the optional external binary pointed at /nonexistent: here boot pre-warmed a live claude child (packs auto-load from the repo, no TUTOR_NO_PREWARM in the test env) and an unhandled ChildProcess 'error' crashed the server on binary-less machines. Also run the suite entry point from a foreign cwd — a spawn("node", ["relative/path"]) inside one test makes the whole hook cwd-sensitive even when every other test resolves paths itself. Both defects fail LOUD (spurious red), not open, but they poison the suite's portability and its own hermeticity claim. | training-companion fix/land-tutor-followups: ps --ppid showed claude -p child; TUTOR_CLAUDE_BIN=/nonexistent → exit 1; test from $HOME → ERR_MODULE_NOT_FOUND

## 2026-08-17

- When a change removes an OkHttp whole-call timeout from a blocking download driven by coroutines, check what else that timeout was silently doing: it is often the ONLY effective cancellation bound, because a blocking execute()/read loop has no suspension points, so coroutine cancel becomes a no-op and the transfer runs to completion. Also check the error wrapper: a runCatching around the call converts the CancellationException thrown at withContext exit into a user-visible "failed" report for work that succeeded.
- 2026-08-17 | merge-commit reconciliation review | To answer "did the merge silently drop anything from either parent" without re-reading the whole branch, recompute the mechanical merge with `git merge-tree --write-tree <p1> <p2>` and diff that tree against the actual merge commit: every file NOT in the diff is provably the untouched auto-merge result, and review effort collapses to the hand-resolved files plus intentional extras (here: one CHANGELOG resolution and a version bump out of ~5700 changed lines). Verify the resolved conflict file by diffing its merged sections against each parent's sections, not by comparing headings. | validated in personal-android-app PR #40 merge-up: git merge-tree reproduced the merge byte-identically except build.gradle.kts + docs/CHANGELOG.md
- 2026-08-17 | Media3 queue/auto-advance review | When reviewing a "handled this STATE_ENDED already" guard that is cleared only on media-item load, enumerate every replay path that does NOT reload: Media3's session layer (MediaSessionImpl.handleMediaControllerPlayRequest -> Util.handlePlayButtonAction, verifiable in the cached AAR bytecode with javap) turns MediaController.play() at STATE_ENDED into seekToDefaultPosition()+play(), so an ended item replays without any load and the guard then eats the second genuine ENDED. Same review, same class of bug: any listener that acts on "current" mutable fields (currentLesson) at ENDED-delivery time instead of the item that ended (the mediaId is right there) mis-attributes the event when delivery races a user-initiated track switch. | training-companion feat/listen-through: finishedLesson guard + onEnded/playNext both bound to live state, replay-without-reload reachable from three UI paths
- 2026-08-17 | media/event-callback persistence review | When persistence hangs off a library's listener callbacks, derive the real dispatch order from the dependency artifact already in the local cache instead of reasoning from idealized docs: unzip the AAR from ~/.gradle/caches and read the queueEvent constant order in javap -c output (Media3 1.5.1 ExoPlayerImpl.updatePlaybackInfo queues EVENT_POSITION_DISCONTINUITY=11 before EVENT_PLAYBACK_STATE_CHANGED=4 before EVENT_IS_PLAYING_CHANGED=7). A "completion" write that is correct in isolation can be silently clobbered by a "stopped" write dispatched later in the same event batch, and only the verified order proves which one lands last. | javap -p -c on media3-exoplayer-1.5.1.aar classes.jar, grep queueEvent
- 2026-08-17 | pre-PR single-commit review | A branch tasked as "one commit, <sha>" can grow commits WHILE the review runs (here a docs commit landed between two of my commands, silently widening scope to AGENTS.md): at the END of the review re-run `git rev-parse HEAD` and `git log <tasked-sha>..HEAD`, review any new commits, and state both SHAs in the report so the caller knows exactly which tree was assured. Also from this review: to close an "exit-0 but wrong artifact" question about a renderer, read the installed CLI's actual main() from the venv's site-packages (piper 1.7.0 writes ALL non-blank stdin lines to the single -f wav and raises on synthesis error), which converts a loose artifact assertion (>5s vs ~25s expected) from a suspected gap into a verified non-path. | wt-hub: git log showed c6c38c0 tip, later rev-parse HEAD=ea2cfcc; ~/.venvs/piper/.../piper/__main__.py lines_to_wav
- 2026-08-17 | infra-wiring (compose/nginx ingress) commit review | When an infra commit wires ingress for content it claims is "already served by an existing bind mount of repo X", verify X's tracking state directly (`git status --porcelain` in the mounted checkout + `ls-remote origin HEAD`): the wiring commit can be pushed, live, and byte-identical to the deploy tree while the ENTIRE page it fronts is untracked working-tree-only files (`?? dir/`) plus an uncommitted doc edit — the commit's own "real files in the repo" premise is then false in git terms, and any clean/checkout/re-clone deletes the live page while the ingress 301 and API service keep running against nothing. Also reusable: with a live checkout on a DIFFERENT branch than the PR base, prove no config reversion by showing base(origin/main) is a strict superset of the live HEAD's version of the touched files, then diff -q live files vs branch tip. | training-hub `?? voices/` + `M AGENTS.md` at HEAD==origin/HEAD while homelab 617def6 deployed live; superset check via git diff ac02f59 039ddd9 -- compose/apps/
- 2026-08-17 | agent-workspace/permission-mode diff review (RCE/secret-exposure surface) | When a fix moves a headless agent's cwd to $HOME to widen its file access, the workspace change is bigger than the diff shows because tools keep cwd-KEYED side-effect stores: `claude` writes per-project history/todos under ~/.claude/projects/<cwd-slug>/, so a per-session-state-dir cwd gave each agent an ISOLATED slug (proof: many `-home-...-appdata-maestro-sessions-<sid>` dirs) while cwd=$HOME collapses every agent AND the operator's own interactive CLI into ONE slug — newly exposing the operator's private transcripts + the auto-memory MEMORY.md to every auth-gated agent, and (with acceptEdits) letting an agent EDIT that memory to poison future operator sessions. Grade "edits only, Bash still refused" against auto-run targets: acceptEdits over a $HOME subtree covers ~/.bashrc, ~/.config/systemd/user/*.service, ~/.local/bin/*, and the server's own source — so edit access converts to DEFERRED code execution at the next shell/timer/restart, and read access now reaches the secret store the bug report itself named (~/appdata keys/keystores, ~/.claude/.credentials.json, ~/.ssh). Confirm the critical no-request-path separately: permission mode read ONLY from env (claudePermissionMode() ignores request; createSession threads model only), frozen set incl bypassPermissions but env-gated. And a cwd-regression test that points herdr at a dead socket only exercises the DIRECT fallback transport (the PANE/herdr production path is untested by the suite) — its mutation proof is real but partial; verify by running new-test-vs-old-server which reproduces the exact promised red. | maestro PR#8 f854e88: ls ~/.claude/projects showed pre-fix per-sid slugs vs post-fix -home-dyadmin; live user-unit env has both knobs commented (defaults $HOME/acceptEdits); B10 green on clean worktree, red when new test run against Phase-5 server (cwd:this.dir)
- 2026-08-18 | systemd unit boot-race fix review | Two systemd interactions decide whether an ExecStartPre wait-loop fix is sound, and both are cheaply testable instead of argued: (1) DefaultTimeoutStartSec (90s on Ubuntu) caps the WHOLE activating phase including ExecStartPre, so a loop longer than that never reaches its own timeout branch or diagnostic message — prove it with a throwaway unit setting TimeoutStartSec=3 over a 30s pre-script (journal shows "start-pre operation timed out", Result=timeout); (2) start-limit (10s/5 default) cannot trip when RestartSec>=3 even for an INSTANTLY-failing ExecStartPre, because at most 4 starts fit one 10s window — prove it with ExecStartPre=/bin/false + Restart=always and watch NRestarts climb past burst while state stays activating(auto-restart). Also: systemd 255 passes $(...) inside a single-quoted sh -c arg through literally (its $-expansion only fires on valid variable-name chars or '{'), verifiable via `systemctl show -p ExecStartPre` on a linked test unit; and sandbox reachability under User=/NoNewPrivileges/ProtectSystem is testable exactly with `sudo systemd-run --wait --pipe -p <same options>`. | user-manager test units dybridge-pre-* / test-timeout / test-restart, 2026-08-18, personal-android-app fix/dy-bridge-boot-bind-race
- 2026-08-18 | shell artifact-verifier review | A numeric artifact gate written as `n="$(grep -c PAT "$FILE" 2>/dev/null || true)"` + `if [ "$n" -ne "$want" ]` fails OPEN exactly when the artifact is MISSING or unreadable: grep prints "0" on zero matches (exit 1, caught by `|| true`) but prints NOTHING on a read error, so `n` is empty and `[ "" -ne want ]` is a test ERROR (status 2) that an `if` treats as false — the failure branch is skipped, a stray "integer expression expected" goes to stderr, and the verifier exits 0 with the artifact absent. Probe every count-based gate by deleting the artifact and asserting the script goes red; the fix class is `n="${n:-}"; [ -n "$n" ] || fail` or an explicit `[ -f "$FILE" ]` precondition. Same review, two cheap positives worth reusing: ffprobe inside a `while read -d '' < <(find -print0)` loop does NOT eat the loop's fd (verified 30/30 iterations), and case-pattern alphabet guards (`*[!A-Za-z0-9_-]*`) on every value interpolated into generated JS make printf/awk emission injection-proof without quoting gymnastics. | gate replica repro in scratchpad: missing manifest -> "OK", exit 0; ai-graph-engineering feat/narration-voice-picker tools/build-all-voices.sh:185-191
- 2026-08-18 | branch review, cache/session invalidation | An invalidation signal only works if every HOLDER of the resolved artifacts compares against it. A generation counter that caches consult but that a long-lived holder of resolved outputs (e.g. a media session's playlist of resolved file URIs, adopted via an id-only equality check) never reads, reopens exactly the staleness the commit's comments claim to close — and the doc comment asserting "the bump makes the holder rebuild" is itself the tell to verify. Separately: a string-splitting test that locates its target with split(marker).at(-1) silently retargets to the LAST occurrence; prove a guard-test real by injecting the regression it names and confirming failure. | verified via node simulation of tests/run.mjs bundle-subject assertion + read of LectureController.holds()/resume()
- 2026-08-18 | pre-merge deploy-risk review (dirty live checkout) | To answer "what will `git merge` do in the LIVE dirty checkout after this lands," don't reason about hunks — replicate the exact state in a scratch CLONE (clone the live repo, `cp` the dirty files in, run the real merge twice: dirty then committed) and read git's actual answer. Two results a diff-read missed: (1) the conflict set included a file OUTSIDE the stated two-file blast radius, because the merge covers the whole branch divergence, not just the files the new commit touches; (2) a block that MOVED between base and one side surfaces as a PAIR of conflict hunks whose incoming sides are empty at one position and full at the other — a mechanical "union / keep both" resolution then DUPLICATES the block, which in first-match-wins config (cloudflared ingress, nginx) silently changes routing; the correct resolution is take exactly one copy after proving the only delta is position (diff the two sides' whole file). Prove the final resolution lossless with one command: whole-tree `git diff <other-parent>` == exactly the expected additions. | scratch clone of homelab: dirty merge => "local changes would be overwritten, Aborting"; committed merge => 3-file conflict incl. cloudflared.yml paired empty-side hunks; resolved tree diff vs branch tip = +57 lines in 2 files, nginx -t OK
- 2026-08-19 | compose service-teardown review | A commit that deletes a compose service does NOT tear the service down under the stated deploy step: plain `docker compose up -d` only prints "Found orphan containers … --remove-orphans" and leaves the removed service's container RUNNING with all its port binds and mounts (verified empirically on docker 29.5.3 with a scratch 2-service project: second `up -d` after deleting one service left it Up). So a teardown review must check three things beyond the diff: the live container (`docker ps` + `ss -tln` on its ports), whether any repo deploy script passes `--remove-orphans` (grep — here none did), and that nothing else shares the mounts being "freed". Also reusable: a pure-deletion diff (0 insertions) proves neighboring blocks untouched by construction — check `--stat` for `0 insertions` before hand-comparing boundaries; and when the live deploy checkout sits on a DIFFERENT branch than the PR base, the cheap conflict-risk bound is `git diff <base> <liveHEAD> -- <touched files>` being empty. | scratchpad orphan-test on docker 29.5.3; homelab 67a177f: apps-voices-api-1 Up 26h after page removal, no --remove-orphans in tree
- 2026-08-19 | exporter CLI review (live-published destination) | Two cheap probes that each found a confirmed defect in a "safe by construction" exporter: (1) when a CLI's integrity assert runs after its write loop, prove persistence with a scratch copy whose input is mutated to trip the assert — here 30 contaminated files landed on disk before the exit-1, and (because the tool skips existing-but-different files without --force) those files then BLOCK the corrected re-run with a message blaming a human author; (2) fuzz every value-taking flag with a MISSING value and with the next flag as its value — `--out` with no arg silently fell back to the live default path, and `--out --dry-run` consumed the safety flag itself, turning a dry run into a real write. Both classes are invisible to green tests because tests exercise the library, not the CLI's arg parse or write ordering. | scratch wt-verify mutation + --dry-run probes, training-companion feat/site-narration-export tools/export-site-narration.mjs
- 2026-08-19 | audio-feature port review (loop-engineering) | When reviewing generated CONTENT (TTS scripts, narration, doc corpora) produced by an exporter in a sibling repo, two cheap corpus-level probes beat per-file reading: (1) grep a character-class of forbidden symbols across the new corpus AND the live precedent corpus the feature is ported from — 30/30 files vs 0/30 exposed a wholesale generator-quality regression in seconds; (2) trace any odd phrase back to its ultimate source (the lesson JS), which turned "2 or more2" from a maybe-style-choice into a confirmed symbol-substitution mangle of "2+2". Also reusable: a runbook number derived by arithmetic (audio-minutes × voices = 16.7 h) is falsified by the sibling repo's MEASURED number for the same job on the same box (70 min) — triangulate ported docs against the precedent's post-measurement docs, not against their own internal consistency. | grep -l '[][{}"=#]' both narration/ trees; js/lessons/d3.js:70 vs narration/d3-l01.txt; graph DEPLOY.md:263 vs loop DEPLOY.md render-time claim
- 2026-08-19 | TTS-narration port review | A fix for one symbol-substitution mangle ("N+" -> "N or more") does not clear the class: the same exporter also silently DELETED "+" and "==" inside diagram lines while preserving them in code-annotation lines, so grep for every operator per transformation context, diff each narration sentence against its source block, and settle "does the TTS speak this symbol at all" empirically by synthesizing "1 + 1" vs "1 1" vs "one plus one" and comparing ffprobe durations. | verified: piper ryan-high 0.99s/0.53s/0.85s; 5 lesson scripts had deleted operators after the declared fix commit
- 2026-08-19 | sibling-port narration review | When a port ships machine-EXPORTED spoken scripts where the sibling shipped hand-written ones, do not stop at symbol-residue greps: render each source block class against its narration. Multi-column ASCII diagrams narrated "top to bottom" glue columns into actively false cross-attributed sentences (the worst class), and an ellipsis-to-pause substitution applied inside quoted code yields degenerate calls like Error(", "). Grep the sibling corpus for the same residue first — its absence there proves the defect is port-introduced, not inherited. Also: always run git status in a review worktree; an uncommitted fix means the branch under review is not the tree you read. | validated: pp-dev-associate feat/site-audio-lectures vs ai-graph-engineering narration/
